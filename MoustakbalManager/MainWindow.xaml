<Window x:Class="MoustakbalManager.MainWindow"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
         TextElement.Foreground="{DynamicResource MaterialDesignBody}"
         TextElement.FontWeight="Regular"
         TextElement.FontSize="13"
         TextOptions.TextFormattingMode="Ideal"
         TextOptions.TextRenderingMode="Auto"
         Background="{DynamicResource MaterialDesignPaper}"
         FontFamily="{DynamicResource MaterialDesignFont}"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MoustakbalManager"
        mc:Ignorable="d"
        Title="Moustakbal" Height="450" Width="800"
        MinHeight="250" MinWidth="400">


    <Grid ScrollViewer.VerticalScrollBarVisibility="Disabled">
        <!-- the transitioner will manage your transitions. notice how SelectedIndex is set to zero: the first slide (instead of the default of -1) -->
        <materialDesign:Transitioner  x:Name="MainTransitioner" SelectedIndex="0" AutoApplyTransitionOrigins="True">
            <Frame x:Name="MainFrame" NavigationUIVisibility="Hidden" />
            <Frame x:Name="AdminFrame" NavigationUIVisibility="Hidden" />
        </materialDesign:Transitioner>
    </Grid>



</Window>